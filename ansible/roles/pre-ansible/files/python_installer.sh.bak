#!/bin/bash

mkdir -p /opt/bin/

cat > /opt/bin/python-wrapper <<EOF
#!/bin/sh

tty > /dev/null 2>&1 && ttyflag='-t'
exec docker run -i --rm -v /home/core:/home/core \$ttyflag --net=none python:2.7-slim python "\$@"
EOF

sudo chmod +x /opt/bin/python-wrapper
